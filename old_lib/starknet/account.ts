import BN from 'bn.js';

import { Address, Felt, KeyPair } from './types';

// async def send_transaction(self, account, to, selector_name, calldata, nonce=None):

// export function send_transaction(
//   signer: KeyPair,
//   account: Address,
//   to: Address,
//   selector_name: 'string',
//   calldata: Felt[],
//   nonce?: number,
// ) {}
